package Arrays;

import java.nio.charset.Charset;
import java.util.Arrays;
import java.util.Scanner;

public class EjeContadorPares {
    public static void main(String[] args) {
        //crear un programa que pida letras las vaya guardando en un array de
        //caracteres luego pide carecter y al final muestra cuantas veces aparece ese caracter.
        Scanner sc = new Scanner(System.in);
        /*char[] letras = new char[10];
        for (int i = 0; i < letras.length; i++) {
            letras[i] = sc.next().charAt(0);
        }
        System.out.println(Arrays.toString(letras));

        System.out.println("Digite un caracter: ");
        char c = sc.next().charAt(0);

        //y al final muestra cuantas veces aparece esa letra.
        int contador = 0;
        for (int i = 0; i < letras.length; i++) {
            if (letras[i] == c){
                contador++;
            }
        }
        System.out.println("la letra " + c + " aparece " + contador + " veces ");*/
    }
}
